# Packages backing the tracked configuration.
#
# One package per line. Blank lines and #-comments are ignored.
# Installed by ./install-packages.sh, which reads this file.
#
# Everything here is in the official/CachyOS repos except monique (AUR),
# which is why the installer uses paru rather than plain pacman.

# --- core: without these the repo cannot bootstrap itself ------------------
stow
git

# --- desktop: backs desktop/niri ------------------------------------------
niri
xwayland-satellite
noctalia-shell
quickshell
monique                     # AUR
polkit-kde-agent            # niri/cfg/autostart.kdl spawns
                            #   /usr/lib/polkit-kde-authentication-agent-1
                            # noctalia's PowerProfile widget needs a
                            # power-profiles-daemon; tlp-pd supplies it, in the
                            # power section below.
wl-clip-persist             # niri/cfg/autostart.kdl spawns it. Wayland ties
                            # clipboard ownership to the window that set it, so
                            # closing the source app empties the clipboard.
                            # cliphist keeps the history either way, but only
                            # reachable through the picker — this is what makes
                            # a plain Ctrl+V still work afterwards.
                            # Note xwayland-satellite above is NOT started from
                            # autostart.kdl and its systemd user unit is
                            # deliberately left disabled: niri 26.04 spawns it
                            # itself. Enabling the unit gives two of them.

# --- shells and prompt: backs common/shell and common/fish ----------------
zsh
zsh-theme-powerlevel10k
fish
fisher
nvm                         # .zshrc and .bashrc source /usr/share/nvm/init-nvm.sh
fzf

# --- terminals and CLI: backs common/terminals and common/cli -------------
kitty                       # .zshrc aliases ssh through `kitty +kitten ssh`
alacritty                   # .profile sets TERM=alacritty
btop
htop
micro
mpv

# --- default applications: backs common/misc/.config/mimeapps.list --------
# Without these the handlers named in mimeapps.list point at nothing.
zen-browser-bin             # default browser: http, https, html, pdf, mailto
sxiv                        # default image viewer: jpeg, png, gif, bmp, webp, svg
nautilus                    # default for inode/directory
visual-studio-code-insiders-bin  # AUR - default editor: text/plain, markdown,
                            #   go, python, js, c, shell, sql; .zshrc aliases
                            #   code to code-insiders
xdgctl                      # AUR - TUI for editing these defaults

# --- theming and environment: backs theme/adw-dark and common/env ---------
adw-gtk-theme               # .profile sets GTK_THEME=adw-gtk3-dark
kvantum
qt5ct                       # .profile sets QT_QPA_PLATFORMTHEME=qt5ct
nwg-look
xsettingsd
ksshaskpass                 # hardcoded in .zshrc and all of environment.d
                            # reaches gnome-keyring via qtkeychain-qt6, which
                            # needs org.freedesktop.secrets, not kwallet

# --- secrets and signing: backs common/gnupg ------------------------------
# gnome-keyring is the only Secret Service on this box; kwallet is uninstalled.
# gpg-agent.conf names pinentry-gnome3 by absolute path, so `pinentry` must be
# present or every signed commit fails once the passphrase cache expires.
# Do NOT switch this to pinentry-qt: it links libKF6GuiAddons, which pacman
# reaps as an orphan the moment nothing KDE is installed.
gnome-keyring
pinentry
libsecret                   # git credential.helper = git-credential-libsecret

# --- docker, rootless: backs the DOCKER_HOST exports in .zshrc/config.fish -
# The daemon runs as the user, not as root. docker-rootless-extras is what
# makes that possible; without it dockerd-rootless.sh is missing and the
# `docker.socket` user unit in meta/systemd-user-units.txt cannot start.
# See "Root-owned pieces" in the README for the subuid/subgid ranges this
# also needs.
docker
docker-rootless-extras      # AUR - dockerd-rootless.sh, rootlesskit, slirp4netns
docker-compose
docker-buildx

# --- power: backs meta/system/tlp.d/10-local.conf --------------------------
# TLP is the power manager, not power-profiles-daemon. On this laptop there is
# no ACPI platform_profile, so PPD had only the amd_pstate knob and reported
# `PlatformDriver: placeholder` for two of its three profiles; TLP also tunes
# disk APM, PCIe ASPM, wifi power save and USB. TLP switches on AC/battery
# natively, which is why no udev rule is needed for it.
tlp
tlp-pd                      # provides= power-profiles-daemon and conflicts
                            #   with it. Ships the UPower.PowerProfiles and
                            #   net.hadess.PowerProfiles D-Bus interfaces, so
                            #   noctalia's widget (Quickshell.Services.UPower)
                            #   keeps working. CLI is tlpctl, NOT
                            #   powerprofilesctl — that binary is gone.
tlpui                       # GTK editor for the drop-in, the way xdgctl edits
                            #   mimeapps.list — changes show up as a diff

# --- maintenance ------------------------------------------------------------
# Back the timers in meta/systemd-system-units.txt. pacman-contrib provides
# paccache.timer, btrfs-progs the scrub timer, snapper and snap-pac the
# snapshots that limine-snapper-sync turns into boot entries. All were already
# installed here; they are listed so a fresh machine gets the timers too.
pacman-contrib
btrfs-progs
snapper
snap-pac
limine-snapper-sync
fwupd                       # firmware updates: fwupdmgr refresh, then
                            #   fwupdmgr update. Nothing else was delivering
                            #   them on this machine. fwupd-refresh.timer in
                            #   meta/systemd-system-units.txt keeps the LVFS
                            #   metadata current.
                            #
                            #   Two things to know before running `update`.
                            #   LVFS has nothing for this laptop's BIOS — HP
                            #   does not publish the Pavilion Gaming 15-ec2xxx
                            #   there — so System Firmware reporting no updates
                            #   means uncovered, not current.
                            #
                            #   And the UEFI dbx update it does offer is
                            #   deliberately not applied. dbx is the Secure
                            #   Boot revocation list, which firmware only
                            #   consults when Secure Boot is on; it is off here
                            #   (SecureBoot efivar 0, no shim, single OS). The
                            #   update is therefore inert, while being awkward
                            #   to undo — clearing dbx means resetting Secure
                            #   Boot keys in BIOS setup — and dbx is the usual
                            #   culprit when fwupd breaks a boot. If Secure
                            #   Boot is ever enabled, apply dbx first and set
                            #   up signing (sbctl) after, not the reverse.

# --- gaming -----------------------------------------------------------------
# This box is hybrid: an AMD Vega iGPU and an RTX 3050. Vulkan enumerates the
# iGPU first, so a game launched without PRIME offload renders on it silently.
# common/bin/.local/bin/gamerun is the wrapper that stops that happening, and
# these are what it composes.
steam
lutris
wine
gamemode                    # performance governor + scheduling for the
                            #   duration of a game, restored on exit. Matters
                            #   here because TLP is deliberately conservative;
                            #   config in common/gaming/.config/gamemode.ini
                            #
                            #   Needs `usermod -aG gamemode $USER` and a
                            #   re-login, or renice silently does nothing —
                            #   the package ships the limits.d permission and
                            #   the group but adds no one to it. See the
                            #   gamemode group note in the README.
lib32-gamemode              # 32-bit games and the Steam runtime need this too,
                            #   or gamemoderun silently does nothing for them
mangohud                    # overlay; its gpu_name field is how you confirm a
                            #   game really is on the NVIDIA card
lib32-mangohud
gamescope
nvidia-prime                # provides prime-run
nvidia-utils
lib32-nvidia-utils
nvidia-settings
                            # xwayland-satellite is in the desktop section
                            # above. It matters here too: niri has no built-in
                            # XWayland, so without it every X11 game and Steam
                            # itself fail to start. niri spawns it when present.

# --- dev tooling ------------------------------------------------------------
# Hooked into the shells and .gitconfig, so installing these changes behaviour
# rather than just adding binaries.
direnv                      # per-directory env from .envrc; hooked in .zshrc,
                            #   .bashrc and config.fish
atuin                       # shell history with fuzzy search, hooked in all
                            #   three shells with --disable-up-arrow
git-delta                   # wired into .gitconfig as core.pager and
                            #   interactive.diffFilter — removing the package
                            #   without editing .gitconfig breaks `git diff`
lazygit
