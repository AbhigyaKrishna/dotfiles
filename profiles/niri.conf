# Profile: niri (Wayland scrolling compositor + noctalia shell)
#
# tier: package [package ...]
#   common  — always-on, desktop-agnostic (any number)
#   theme   — GTK/Qt look (pick one)
#   desktop — compositor / DE (pick one)

common:  shell fish git terminals cli env misc bin systemd
theme:   adw-dark
desktop: niri
