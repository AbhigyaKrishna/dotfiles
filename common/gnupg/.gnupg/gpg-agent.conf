# pinentry-qt broke when kwallet removal orphaned kguiaddons (libKF6GuiAddons).
# gnome3 backend matches the gnome-keyring consolidation and uses gcr-prompter.
pinentry-program /usr/bin/pinentry-gnome3
